name: WIP

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  godwoken-tests:
    uses: keroro520/godwoken-tests/.github/workflows/reusable-integration-test-v1.yml@ci-allow-fork-repo
    with:
      extra_github_env: |
        KICKER_REPO=${{ github.repository }}
        KICKER_REF=${{ github.ref }}
        HELLO=a
